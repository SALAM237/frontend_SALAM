/* Tailwind v4: primary configuration lives in app/globals.css via @theme {}.
   This file is kept for IDE tooling compatibility. */
import type { Config } from 'tailwindcss';

export default {} satisfies Config;
